#pragma once
/* #include <Windows.h>
#include <string>
#include <initializer_list>
#include "include.hpp"

namespace clantag
{
	extern void vitamins(CUserCmd* pCmd);
	extern void FindPattern();
	std::uint8_t* PatternScan(void* module, const char* signature);
} */